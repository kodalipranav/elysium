import 'package:flutter/material.dart';

class CompletedPosts extends StatelessWidget{
  const CompletedPosts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Completed")),);
  }
}